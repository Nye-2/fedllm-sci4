We presented \method, a framework for privately federating an edge small language model as an evolving IIoT security assistant. The method uses only released backbone weights to construct fixed spectral adapter bases, trains small core matrices, and applies client-level DP to uploaded core updates. Its design targets the utility loss caused by client-side release DP through public spectrum-driven allocation, layer-adaptive noise, local residual personalization, and post-processing. The planned evaluation on \dataset is structured around fair budget matching, a strong Fed-SB-style baseline, classical IDS context, and leakage-aware reproducibility. \todo{Replace with result-backed conclusion after experiments.}
