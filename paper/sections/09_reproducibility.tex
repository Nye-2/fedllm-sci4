The codebase fixes dataset split artifacts, label maps, prompt rendering code, client partitions, and experiment configuration files. The main seed is 20260531. Experiments should report the exact backbone checkpoint and revision, target modules, rank allocation, client participation schedule, privacy accountant, RDP order grid, clipping radii, noise scales, hardware, package versions, and command lines. Raw data should not be committed to the paper repository; server handoff should follow the repository-level \texttt{HANDOFF.md}.
